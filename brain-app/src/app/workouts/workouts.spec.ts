import{TestBed}from'@angular/core/testing';import{provideRouter}from'@angular/router';import{Workouts}from'./workouts';import{Workout,WorkoutsService}from'./workouts.service';
describe('Workouts',()=>{const workout:Workout={id:'1',user_id:'u',workout_type:'running',workout_date:'2026-07-22',workout_name:null,distance_km:5,duration:'40:14',pace:'8:02',avg_heart_rate:141,elevation_m:null,reps:null,stationary:null,run_type:'Long Run',notes:null};const service={load:vi.fn().mockResolvedValue({workouts:[workout],sportTypes:[]}),saveWorkout:vi.fn(),deleteWorkout:vi.fn()};beforeEach(async()=>{await TestBed.configureTestingModule({imports:[Workouts],providers:[provideRouter([]),{provide:WorkoutsService,useValue:service}]}).compileComponents();});it('renders real workout fields',async()=>{const f=TestBed.createComponent(Workouts);await f.whenStable();f.detectChanges();const text=f.nativeElement.textContent;expect(text).toContain('Running');expect(text).toContain('5 km');expect(text).toContain('Pace: 8:02');expect(text).toContain('141 bpm');expect(text).toContain('22.7.2026');});});
