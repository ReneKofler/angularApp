import {TestBed} from '@angular/core/testing';import {provideRouter} from '@angular/router';import {vi} from 'vitest';import {AuthService} from '../core/auth.service';import {Groceries} from './groceries';import {GroceriesService} from './groceries.service';
describe('Groceries',()=>{const owned={id:'l1',user_id:'u1',name:'Woche',created_at:'2026-01-01'},shared={id:'l2',user_id:'u2',name:'Party',created_at:'2026-01-01',is_shared:true};const service={load:vi.fn().mockResolvedValue({lists:[owned,shared],items:[],shares:[]}),createList:vi.fn(),renameList:vi.fn(),deleteList:vi.fn(),createItem:vi.fn(),toggleItem:vi.fn(),deleteItem:vi.fn(),share:vi.fn(),removeShare:vi.fn()};beforeEach(async()=>{vi.clearAllMocks();await TestBed.configureTestingModule({imports:[Groceries],providers:[provideRouter([]),{provide:GroceriesService,useValue:service},{provide:AuthService,useValue:{session:vi.fn().mockReturnValue({user:{id:'u1',email:'me@test.dev',user_metadata:{}}})}}]}).compileComponents()});it('shows owner controls only for owned lists',async()=>{const f=TestBed.createComponent(Groceries);await f.whenStable();f.detectChanges();expect(f.nativeElement.querySelectorAll('[aria-label="Liste teilen"]').length).toBe(1)});it('creates items for shared lists',async()=>{const f=TestBed.createComponent(Groceries);await f.whenStable();f.componentInstance.itemNames.set({l2:'Chips'});await f.componentInstance.addItem(shared);expect(service.createItem).toHaveBeenCalledWith('l2','Chips')})});
